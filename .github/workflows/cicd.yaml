name: Build and push docker image

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Check out the repository
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # Login to ECR
      # ===========
      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Docker build
        run: |
          docker build --build-arg REACT_APP_API_URL=http://32.198.35.8:8080/api/v1 -t movie-frontend-2:latest .

      - name: Docker push
        run: |
          docker tag movie-frontend-2:latest 317706704902.dkr.ecr.us-east-2.amazonaws.com/movie-frontend-2:latest
          docker push 317706704902.dkr.ecr.us-east-2.amazonaws.com/movie-frontend-2:latest
